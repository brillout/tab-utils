{
  "name": "tab-utils",
  "version": "0.0.2",
  "scripts": {
    "build": "parcel build src/*.js",
    "prepublishOnly": "yarn build && cp -r ./dist/* ./",
    "postpublish": "git clean -fd"
  },
  "devDependencies": {
    "parcel": "^1.12.4"
  }
}
